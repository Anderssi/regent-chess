ANTHROPIC_API_KEY=
# Optional overrides
CLAUDE_MODEL=claude-opus-5
# Path to a native Stockfish binary. If unset, uses `stockfish` on PATH, else the bundled WASM build via node.
STOCKFISH_PATH=
PORT=3000
DB_PATH=data/regent.sqlite
